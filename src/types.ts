export type Esya = {
    id: string
    user: string,
    esya: string,
    location?: string,
    category?: string
    image?: string
    description?: string
    isSelling?: boolean
    isTrading?: boolean
    isLending?: boolean
}
