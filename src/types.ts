export type Esya = {
    id: string,
    esya: string,
    location?: string,
    contact?: string
    category?: string
    image?: string
    description?: string
    isMissing?: boolean
    isFound?: boolean
}
